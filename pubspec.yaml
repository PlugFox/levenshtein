name: levenshtein
description: Damerau–Levenshtein on Dart — baseline from Habr article + a ladder of optimizations and benchmarks.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.5.0

dev_dependencies:
  benchmark_harness: ^2.3.1
  test: ^1.25.0
  lints: ^5.0.0
